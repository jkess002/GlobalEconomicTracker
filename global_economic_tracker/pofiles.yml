global_economic_tracker:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      user: airflow_pipeline
      password: airflow_pipeline
      port: 5432
      dbname: airflow_pipeline
      schema: public
      threads: 4
